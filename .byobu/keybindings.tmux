unbind-key -n C-a
set -g prefix ^A,F12
set -g prefix2 ^A
bind a send-prefix
unbind-key -n C-Left
unbind-key -n C-Right

# # bind-key -n C-Left backward-word
# bind-key -n C-e select-pane -t 1
# # bind C-Right forward-word
# bind-key -n C-Left backward-word
